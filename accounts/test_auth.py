"""Legacy duplicate test module kept empty as a placeholder.

The active tests live in `accounts/tests.py`. This file prevents
unittest discovery from treating a duplicate module with the same
test classes differently on some environments.
"""
